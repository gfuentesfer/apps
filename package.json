{
  "name": "myarrowsapp",
  "version": "1.0.0",
  "private": true,
  "description": "Entrypoint Render cuando Root Directory está vacío (usa server/)",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "npm start --prefix server"
  },
  "engines": {
    "node": ">=20"
  }
}
