{
  "name": "myarrowsapp",
  "version": "1.0.0",
  "private": true,
  "description": "MyArrows Flutter app — base de datos SQLite embebida",
  "scripts": {
    "db:build": "npm install --prefix scripts && node scripts/build-sqlite-db.js"
  }
}
